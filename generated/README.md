# Generated Figma Source

这里保存由 Figma 插件同步的全量原始数据源。

默认文件：

- `figma-token-source.json`

维护约定：

- 这个目录下的 JSON 由工具生成，不手工编辑。
- 如果需要追溯 alias、mode、组件级标记或 Figma 原始变量，优先读这里。
- 面向开发侧的 CSS/Flutter 精简产物不要直接写在这里，应放到 `artifacts/css`、`artifacts/flutter` 或未来 exporter 输出目录。
