{
  "name": "hl-scene-design-system",
  "version": "0.1.0",
  "private": true,
  "description": "HL Scene design system monorepo for Figma tokens, design rules, generated assets, and Flutter components.",
  "type": "module",
  "main": "packages/design-system-core/dist/index.js",
  "types": "packages/design-system-core/dist/index.d.ts",
  "files": [
    "packages/design-system-core/dist"
  ],
  "scripts": {
    "build": "npm --prefix packages/design-system-core run build",
    "typecheck": "npm --prefix packages/design-system-core run typecheck",
    "docs:dev": "pnpm --dir apps/docs site:dev",
    "docs:build": "pnpm --dir apps/docs site",
    "figma:check": "npm --prefix apps/figma-token-sync-plugin run check",
    "figma:build": "npm --prefix apps/figma-token-sync-plugin run build"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.3"
  }
}
