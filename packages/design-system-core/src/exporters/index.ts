/**
 * Exporter boundary.
 *
 * Future modules here should convert the parsed design-system snapshot into
 * consumer-specific artifacts such as AI Markdown, CSS variables, Flutter
 * theme files, docs-site data, or MCP datasets.
 */
export const DESIGN_SYSTEM_EXPORTER_LAYER = 'exporters';
